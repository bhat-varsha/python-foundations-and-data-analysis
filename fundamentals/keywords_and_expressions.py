#keyword="thse are word which we canot change"
#Keywords are reserved words in Python that have predefined meaning
#and cannot be used as variable names.
print(help("keywords")) # it will give all the keywords present in the python
#examples of keywords: if, else, for, while, True, False, import, return, def, class
#(No syntax; they are predefined words)

#syntax of keywords, means how to call the keywords
"""if condition:
for variable in sequence:
while condition:
def function_name():
class ClassName:
return value
import module_name

"""
#exmaples
if True:
    print("Keyword example")





# (combination of varaible, value and operators )
#An expression is a combination of values, variables, and operators that gives a result
        #expression means valid piece of code that produces a value
x = (10 + 5) * 2
b=1+2 #thses x and b are both expression bcz both produces a value
c="varsha_hg" #this is also an exoression it is producing a value which is string
x=10 #it is not a expression it is a statemnt
#the above line is not producing any value ,just perfomrs the action
"""
An expression is any code that evaluates to a value (e.g., 10, "hello", 5+3, len("abc")).
It always evaluates to something
Can be printed or assigned
Does not perform an action by itself

Examples of Expressions
5 + 3
a * b
"Hello " + "Varsha"
10 > 5

 
A statement is a complete instruction that performs an action 
but does not itself return a value (e.g., x = 10, if, for, while). 
Used to create variables, loops, conditions, etc.
Examples of Statements
x = 10          # assignment statement
print(x)        # print statement
if x > 5:       # conditional statement
    print("Big")
These do work, not just calculate a value.

Therefore, 10 is an expression because it evaluates to a value, 
while x = 10 is an assignment statement 
because it stores a value and does not itself return any value.

Simple Example Showing Both
y = 5 + 2   # 5 + 2 is an EXPRESSION
            # y = 5 + 2 is a STATEMENT
            
#every line in a python is a statement
#An expression can exist inside a statement,
# but an expression alone is not considered a full program line unless Python treats it as a statement.
"""
#synatx of expression
#operand operator operand
#operand = value, variable, literal, or function output
#operator  +, -, *, /, %, ==, and, or, etc.

x = (10 + 5) * 2






